Django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
openpyxl>=3.1
python-docx>=1.1
requests>=2.31
# Signature RSA des requêtes vers la plateforme de paiement Makuta
# (common/makuta.py). Sans elle, aucun paiement ne peut être authentifié.
cryptography>=42
